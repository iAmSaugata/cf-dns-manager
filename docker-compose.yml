version: "3.9"
services:
  app:
    build: .
    image: cf-dns-manager:latest
    ports:
      - "8080:8080"
    environment:
      - APP_PASSWORD=${APP_PASSWORD}
      - CF_API_TOKEN=${CF_API_TOKEN}
      - PORT=8080
    restart: unless-stopped
